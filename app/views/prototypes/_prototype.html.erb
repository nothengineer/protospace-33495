<div class="card">
  <%# <%= link_to image_tag(prototype.image,class: :card__img),"/prototypes/#{prototype.id}" %> 
  <div class="card__body">
    <%=  link_to "#{prototype.date}", prototype_path(prototype.id,user_id:prototype.user_id), class: :card__title%>
    <p class="card__summary">
      <%= "#{prototype.thema}" %>
    </p>
    <%= link_to "by #{prototype.user.name}", "/users/#{prototype.user.id}}", class: :card__user %>
  </div>
</div>